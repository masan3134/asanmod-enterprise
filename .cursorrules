---
description: ASANMOD v3.2.0 Agent Constitution
globs: *
---

<!--
ASANMOD v3.2.0 - DOCUMENTATION HEADER
ID: CONST-004
STATUS: ACTIVE
TYPE: RULES
LAST_UPDATED: 2026-01-18 (UTC)
VERSION: v3.2.0
-->

# ASANMOD v3.2.0 AGENT CONSTITUTION (EXECUTABLE RULES)

> **PHILOSOPHY:** Rules are enforced by scripts, not documents.

## 🚨 HARD CONSTRAINTS (PHYSICALLY ENFORCED)

| Constraint | Enforcer |
|------------|----------|
| Commit format: `type(scope): message` | `.husky/commit-msg` REJECTS |
| No `console.log` in backend | `.husky/pre-commit` REJECTS |
| MCP-only file ops | `verify-core.cjs` blocks |
| **version.json DOKUNMA** | `deploy-prod.sh` manage |
| **Read asanmod-core.json FIRST** | **MANDATORY** |

## 🛡️ IRON CURTAIN (ISOLATION)

| Environment | PM2 Name | Port | Access |
|-------------|----------|------|--------|
| **DEV** | `app-dev` | **3000** | IP (Hot Reload) |
| **PROD** | `app-prod` | **3002** | Stable (PROD) |

**RULE:** NEVER touch Prod files manually. Use `npm run deploy-prod`.

## ⚡ v3.2.0 AUTOMATION (USE THESE!)

### PM2 Commands (v10.0 AI-Responsive)

```bash
./scripts/mod-tools/pm dev status           # Check status
./scripts/mod-tools/pm dev errors           # 🤖 Error scan (UTC+3)
./scripts/mod-tools/pm prod diag            # 🤖 Diagnostic report
./scripts/mod-tools/pm dev health           # 🤖 Service health
```

### Operations

```bash
npm run verify    # Quality check (0/0/0)
npm run fix       # Auto-fix lint
npm run deploy-prod  # Production deploy
```

## 📖 SINGLE REFERENCE
**Read ONLY this:** `docs/AGENT_QUICK_REF.md`

## 🔒 THE ZERO TOLERANCE DIRECTIVE
- **NO TERMINAL IO:** Use `view_file`, `read_file`.
- **QUALITY:** `0/0/0` (Enforced by `verify-core.cjs`).
- **LANGUAGE:** TURKISH (Always, except for code).

## 🟢 INITIALIZATION
1. Read `docs/AGENT_QUICK_REF.md`
2. Run `npm run verify`
3. Report: "**ASANMOD v3.2.0 Online.**"
