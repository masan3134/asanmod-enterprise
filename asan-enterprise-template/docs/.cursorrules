---
description: ASANMOD v1.1.1 Agent Constitution
globs: *
---

<!--
ASANMOD v1.1.1 - DOCUMENTATION HEADER
ID: CONST-004
STATUS: ACTIVE
TYPE: RULES
LAST_UPDATED: 2026-01-13 (UTC)
VERSION: v1.1.1
-->

# ASANMOD v1.1.1 AGENT CONSTITUTION (EXECUTABLE RULES)

> **PHILOSOPHY:** Rules are enforced by scripts, not documents.

## 🚨 HARD CONSTRAINTS (PHYSICALLY ENFORCED)

| Constraint | Enforcer |
|------------|----------|
| Commit format: `ID: <TASK> \| <Message>` | `.husky/commit-msg` REJECTS |
| No `console.log` in backend | `.husky/pre-commit` REJECTS |
| MCP-only file ops | `verify-core.cjs` blocks |
| **version.json DOKUNMA** | `deploy-prod.sh` manage |
| **Read asanmod-core.json FIRST** | **MANDATORY** |

## 🛡️ IRON CURTAIN (ISOLATION)

| Environment | PM2 Name | Port | Access |
|-------------|----------|------|--------|
| **DEV** | `app-dev` | **3000** | IP (Hot Reload) |
| **PROD** | `app-prod` | **3002** | Domain (Stable) |

**RULE:** NEVER touch Prod files manually. Use `npm run deploy-prod`.

## 🔴 API DOĞRULAMA PROTOKOLÜ

> **HER değişiklik curl ile test edilmeli!**

1. `docs/CREDENTIALS.md` oku.
2. `curl` ile TOKEN al.
3. 200 OK alana kadar test et.

## ⚡ v1.1.1 AUTOMATION (USE THESE!)

### PM2 Commands (NEVER write full names!)

```bash
./scripts/mod-tools/pm prod status           # All services
./scripts/mod-tools/pm dev logs frontend     # Dev frontend logs
```

### Operations

```bash
npm run verify    # Quality check (0/0/0)
npm run fix       # Auto-fix lint
npm run deploy-prod  # Production deploy
```

## 📖 SINGLE REFERENCE

**Read ONLY this:** `docs/ASANMOD_DOC_REGISTRY.md`

## 🔒 THE ZERO TOLERANCE DIRECTIVE

- **NO TERMINAL IO:** Use `view_file`, `read_file`.
- **QUALITY:** `0/0/0` (Enforced by `verify-core.cjs`).
- **LANGUAGE:** TURKISH (Always, except for code).

## 🟢 INITIALIZATION

1. Read `GEMINI.md` / `CURSOR.md`
2. Report: "**ASANMOD v1.1.1 Online.**"
